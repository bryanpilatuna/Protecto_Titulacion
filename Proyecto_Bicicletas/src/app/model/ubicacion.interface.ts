export interface datosUbicacion {
    id?: string;
    position: {
        latitude: number,
        longitude: number,
      };
      title: string;
      direccion: string;
      telefono: number;
}